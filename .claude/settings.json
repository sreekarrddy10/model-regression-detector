{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/format-python.sh\""
          }
        ],
        "description": "Format Python with black + isort + ruff --fix after any edit, then type-check the package. Non-blocking: a formatter that blocks an edit is worse than an unformatted file."
      }
    ]
  }
}
